# Loaded by `vite build --mode single`. The single-file bundle is opened from
# disk with no server, so it needs the hash router — and setting it through a
# mode file rather than an inline env prefix keeps `npm run build:single`
# working in PowerShell, where `VAR=1 cmd` isn't valid syntax.
VITE_HASH_ROUTER=1
